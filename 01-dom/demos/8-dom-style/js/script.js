const init = () => {
  const $title = document.querySelector(`.title`);

  // is empty becaus there is no inline CSS
  console.log($title.style);

  // set a different backgroundcolor
  //$title.style.backgroundColor = `blue`;
};

init();
